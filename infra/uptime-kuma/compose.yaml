services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    restart: unless-stopped
    volumes:
      - uptime_kuma_data:/app/data
    networks:
      - proxy

volumes:
  uptime_kuma_data:

networks:
  proxy:
    external: true
    name: homeops-proxy
